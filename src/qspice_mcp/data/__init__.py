"""Bundled package data for qspice-mcp.

Recipes live under ``data/recipes/{recipe_id}/``:

- ``recipe.json`` — artifacts manifest + workflow instruction index
- artifact files (``.qsch``, ``.cpp``, …)
- workflow markdown (``scratch.md``, ``catalog.md``, …)

Add a recipe: create the folder, edit ``data/recipes/index.json``.
"""
